import { Box, BoxProps } from '@auspices/eos'
import React from 'react'

type CollectionContentsListProps = BoxProps & {}

export const CollectionContentsList: React.FC<CollectionContentsListProps> = ({
  ...rest
}) => {
  return <Box {...rest}>todo</Box>
}
